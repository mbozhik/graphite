---
interface Props {
  text: string
  classes?: string
}

const {text, classes} = Astro.props as Props
---

<h3 class="text-xl font-medium uppercase sm:text-base sm:leading-snug" class:list={classes} set:html={text} />
